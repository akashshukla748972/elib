import React from "react";
import MainRouting from "./utils/MainRouting";

const App = () => {
  return (
    <div
      className={`min-h-screen flex flex-col overflow-x-hidden bg-gradient-to-b from-gray-50 via-blue-50 to-gray-100`}
      r
    >
      <MainRouting />
    </div>
  );
};

export default App;
